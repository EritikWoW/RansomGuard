{
  "version": "0.7.3.0",
  "scope": "automated Windows userspace validation plus compile-only x64 minifilter validation",
  "performed": [
    "validated Directory.Build.props and all Windows manifests at version 0.7.3.0",
    "ran normal plus Engineering LAB userspace build on GitHub Actions Windows runner",
    "ran protocol v4 minifilter, rollback, administration, localization and recovery source gates",
    "ran 56 rollback/store policy tests including CREATE disposition/options and FILE_DELETE_ON_CLOSE cases",
    "ran WPF synthetic smoke rendering for 130 frames with no recorded binding errors",
    "published normal and Engineering LAB 0.7.3.0 userspace ZIPs",
    "restored pinned Microsoft Windows WDK x64 and SDK C++ 10.0.28000.2526 packages",
    "compiled and linked RansomGuardMinifilter.sys as Release x64 with signing disabled",
    "ran x64 ApiValidator against the compiled SYS and received Universal DDI validation success",
    "verified the compile-only minifilter artifact is not Authenticode-signed",
    "uploaded the unsigned SYS, INF, protocol header and SHA256SUMS as a CI artifact"
  ],
  "not_performed": [
    "driver installation, loading or volume attachment",
    "live FilterGetMessage/FilterReplyMessage kernel-to-user round trip",
    "native IRP_MJ_CREATE or IRP_MJ_WRITE blocking test on NTFS/ReFS",
    "post-create tunneled-name and durable volume/file-ID reconciliation",
    "Driver Verifier, HLK or WHCP validation",
    "power-loss or kernel/user-mode crash recovery",
    "memory-mapped and cache-manager write coverage",
    "performance and tail-latency benchmarking under real filesystem load",
    "production driver signing or Microsoft-assigned production altitude"
  ],
  "safety_boundary": "The blocking minifilter remains Engineering-LAB-only. CI compiles and validates an unsigned artifact but never installs, loads or attaches it. The normal bundle remains AuditOnly.",
  "next_required_checkpoint": "Run the validated unsigned driver only in a disposable Windows VM and disposable gate directory, then prove live protocol-v4 CREATE/WRITE ordering and post-create identity reconciliation before expanding the kernel gate."
}
