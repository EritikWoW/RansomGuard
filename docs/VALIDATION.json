{
  "version": "0.7.11.0",
  "scope": "automated Windows userspace validation plus compile-only x64 minifilter validation",
  "validated_source_sha": "b0fc6c2e4975c34c691f75c94df30e0f26596f1c",
  "evidence": {
    "pull_request": 17,
    "windows_build_run": 35624906576,
    "minifilter_compile_run": 35624906552
  },
  "performed": [
    "validated Directory.Build.props and all Windows manifests at version 0.7.11.0",
    "ran protocol v9 minifilter, rollback, administration, localization, recovery and read-only UI/API source gates",
    "ran 128 rollback/store tests including write-capable CREATE policy, durable DesiredAccess intent validation/reopen, paging evidence, restart evidence and journal corruption rejection",
    "ran 125 administration contract tests",
    "ran 2194 localization resource/contract checks",
    "ran WPF synthetic smoke rendering for 130 frames with no recorded binding errors",
    "published normal and Engineering LAB 0.7.11.0 userspace ZIPs",
    "validated CREATE ordering for content-write capable existing-file opens: FILE_ID_INFO binding and full pre-image commit precede durable CREATE intent and allow",
    "validated read-only CREATE remains non-eager and originally-absent incident paths remain absence-governed",
    "compiled and linked RansomGuardMinifilter.sys as Release x64 with protocol v9 and signing disabled",
    "ran x64 ApiValidator against the compiled SYS and received Universal DDI validation success",
    "verified the compile-only minifilter artifact is not Authenticode-signed",
    "uploaded the unsigned SYS, INF, protocol header and SHA256SUMS as a CI artifact"
  ],
  "not_performed": [
    "driver installation, loading or volume attachment",
    "live FilterGetMessage/FilterReplyMessage kernel-to-user round trip with the compiled driver",
    "native writable memory-mapping test proving a write-capable CREATE receives pre-image before CreateFileMapping(PAGE_READWRITE)",
    "section-synchronization attestation for writable mapping creation",
    "coverage for writable mappings created from file handles that existed before LAB gate activation",
    "live crash/restart fault injection between intent commit and authoritative kernel completion delivery",
    "production driver signing or Microsoft-assigned altitude validation"
  ],
  "safety_boundary": "The blocking minifilter remains Engineering-LAB-only. CI compiles and validates an unsigned artifact but never installs, loads or attaches it. The normal bundle remains AuditOnly.",
  "next_required_checkpoint": "Run the validated unsigned protocol-v9 driver only in a disposable Windows VM and disposable gate directory; prove write-capable CREATE pre-image ordering followed by a live writable mapping and PagingWrite evidence, then add section-synchronization attestation without blocking memory-manager callbacks."
}
